"use client";

import SiteLayout from "@/components/layout/SiteLayout";
import InnerPage from "@/components/layout/InnerPage";

export default function ServicesPage() {
  return (
    <SiteLayout>
      <InnerPage title="Services">
        <p>
          Ask about fundamental rights, equality, arrest and detention,
          expression, and other constitutional topics — with references to the
          articles that matter.
        </p>
      </InnerPage>
    </SiteLayout>
  );
}
